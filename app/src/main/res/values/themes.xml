<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style
        name="Theme.WEATHER_FORECAST"
        parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item> <!-- Fullscreen -->
        <item name="android:windowNoTitle">true</item> <!-- Không có title bar -->
        <item name="android:windowIsTranslucent">false</item> <!-- Không nền mờ -->
        <item name="android:windowBackground">@android:color/black</item> <!-- Nền đen -->
    </style>
</resources>